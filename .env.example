# Environment variables for Weather Backend Service

# Google Cloud Storage bucket name (required)
GCS_BUCKET_NAME=your-weather-data-bucket

# Google Cloud Project ID
GOOGLE_CLOUD_PROJECT=your-project-id

# Port for local development (optional, defaults to 8080)
PORT=8080

# For local development only - path to service account key file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json
